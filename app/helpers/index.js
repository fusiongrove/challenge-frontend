const helpers = {
  httpHelper: require('./httpHelper'),
  uploadHelper: require('./uploadHelper'),
  storageHelper: require('./storageHelper'),
  utilsHelper: require('./utilsHelper')
};

module.exports = helpers;